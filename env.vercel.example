# Vercel Production Environment Variables
# 1. Create a new project on Vercel and link it to your GitHub repository.
# 2. Go to your Vercel project's "Settings" -> "Environment Variables".
# 3. Add each of these variables with their corresponding values.
# 4. For sensitive values, ensure they are set as "Encrypted".

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# The public URL of your Vercel deployment.
NEXT_PUBLIC_APP_URL=https://your-project-name.vercel.app
# The authentication URL for NextAuth.js. Should be the same as NEXT_PUBLIC_APP_URL.
NEXTAUTH_URL=https://your-project-name.vercel.app
# The environment should be set to production.
NODE_ENV=production

# ===========================================
# AUTHENTICATION
# ===========================================

# A secret key for NextAuth.js to sign tokens.
# Generate a 32+ character random string.
NEXTAUTH_SECRET=

# ===========================================
# PAYLOAD CMS CONFIGURATION
# ===========================================

# A secret key for Payload CMS to sign tokens.
# Generate a 32+ character random string.
PAYLOAD_SECRET=
# The public URL of your Payload CMS instance. Should be the same as NEXT_PUBLIC_APP_URL.
PAYLOAD_PUBLIC_SERVER_URL=https://your-project-name.vercel.app

# ===========================================
# SUPABASE DATABASE & AUTH
# ===========================================

# Create a new project on Supabase (https://supabase.com).
# Find these values in your Supabase project's "Settings" -> "API".

# The public URL of your Supabase project.
NEXT_PUBLIC_SUPABASE_URL=
# The anonymous public key for your Supabase project.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# The service role key for your Supabase project. Keep this secret!
SUPABASE_SERVICE_ROLE_KEY=

# The connection string for your Supabase PostgreSQL database.
# Find this in your Supabase project's "Settings" -> "Database" -> "Connection string".
# Use the "URI" format.
DATABASE_URL=

# ===========================================
# VERCEL KV (REDIS)
# ===========================================

# Create a new KV store in your Vercel project's "Storage" tab.
# Copy the connection string.
KV_URL=

# ===========================================
# VERCEL BLOB STORAGE
# ===========================================

# Create a new Blob store in your Vercel project's "Storage" tab.
# Copy the read-write token.
BLOB_READ_WRITE_TOKEN=

# ===========================================
# STRIPE PAYMENTS
# ===========================================

# Your Stripe secret key.
STRIPE_SECRET_KEY=
# Your Stripe publishable key.
STRIPE_PUBLISHABLE_KEY=
# Your Stripe webhook signing secret.
STRIPE_WEBHOOK_SECRET=

# ===========================================
# EMAIL (SMTP)
# ===========================================

# We recommend using a transactional email service like SendGrid or Mailgun.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
